version: "3.9"

services:
  next-app:
    container_name: next-app
    build:
      context: .
      dockerfile: Dockerfile.prod
    env_file:
      - .env.production
    restart: always
    ports:
      - 3000:3000
